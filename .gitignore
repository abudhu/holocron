# Rust
/target/
**/*.rs.bk
Cargo.lock.bak

# IDE
/.idea/
/.vscode/
*.swp
*.swo
.zed/

# macOS
.DS_Store

# Hermes / agent scratch
/.hermes/
/.claude/
/.cursor/

# Holocron's own audit reports — dogfood + `holocron audit .` runs
# now write to .holocron/reports/<ts>.{md,json,sarif,html} under the
# audited project. Keep the dir itself out of git; the tool recreates
# it on demand.
/.holocron/

# Direnv
.envrc.local
